# Changelog

## v1.1.0 - Theme selector update
- Added 5 niche-specific colour schemes.
- Added a visible colour scheme selector panel with named options.
- Saved the selected theme with localStorage for returning visitors.
- Refactored the template to use CSS variable-based theme overrides.

## v1.0.0 - Initial release
- Initial static template release.
